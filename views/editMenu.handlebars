<article>
        <h2>editing {{menu.title}}</h2>
        <form action="/restaurants/{{restaurant.id}}/menus/{{menu.id}}/edit" method="POST">
            <div>
                <h4 style="margin-left:1rem;">title</h4>
                <input class="input1" type="text" name = "name" placeholder="restaurant name" value="{{menu.title}}" required/><br>
                <button class='submit1'>update</button>
            </div>

        </form>
</article>
